package calculatordemo;

/**
 * 
 * interface is a specification in which we mention our rules(methods)
 * 
 * interface is used to create a standard that Implementing classes will follow
 * 
 * methods are by default public in interface
 * 
 */
public interface ICalculator {
	int sub(int num1, int num2);
	
	int add(int num1, int num2);
	
}
